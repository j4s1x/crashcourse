#make a list of the numbers one to one million, then use min() max()to make sure it starts
#at one and ends at 1 million.  also use the sum() function to see how long it takes python
#to add the numbers.

numbers=list(range(1,1000001))
print(numbers)
print(sum(numbers))
print(min(numbers))
print(max(numbers))
